<!DOCTYPE html>
<html>
  <head>
    <title>Oink</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="flex justify-between items-center p-4">
      <div class="flex items-center space-x-4">
        <%= image_tag("piggy-bank.svg", alt: "oink's piggy bank logo") %>
        <h1 class="text-emerald-400 text-3xl">oink</h1>
      </div>
      <div class="flex space-x-4 items-center">
        <% if user_signed_in? %>
          <p>Welcome, <%= current_user.email %>!</p>
          <%= 
            link_to 'Edit profile', 
            edit_user_registration_path, 
            class: "bg-emerald-400 hover:bg-emerald-500 text-zinc-900 font-bold py-2 px-4 rounded"
          %>
          <%= 
            link_to 'Sign out', 
            destroy_user_session_path, 
            method: :delete,
            class: "bg-emerald-400 hover:bg-emerald-500 text-zinc-900 font-bold py-2 px-4 rounded"
          %>
        <% else %>
          <%= 
            link_to 'Sign up', 
            new_user_registration_path, 
            class: "bg-emerald-400 hover:bg-emerald-500 text-zinc-900 font-bold py-2 px-4 rounded"
          %>
          <%= 
            link_to 'Sign in', 
            new_user_session_path,
            class: "bg-emerald-400 hover:bg-emerald-500 text-zinc-900 font-bold py-2 px-4 rounded"
          %>
        <% end %>
      </div>
     
      <div>
        <p class="notice text-orange-400 text-sm"><%= notice %></p>
        <p class="alert text-red-400 text-sm"><%= alert %></p>
      </div>
    </header>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
